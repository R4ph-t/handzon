---
export const prerender = true;
import "~/styles/global.css";
import HomePage from "@handzon/ui/pages/Home.astro";
import { site } from "~/config/site";
---
<HomePage
  siteName={site.name}
  tagline={site.tagline}
  hero={site.hero}
  showResumeRail={site.showResumeRail}
/>
